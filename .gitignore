node_modules/
dist/
data/
output/
*.log
.env
.env.bak
config.json
*.local
.DS_Store
# Local npm config (machine-specific prefix path; never commit to the public repo)
.npmrc

# Local TLS material (private key must never be committed)
tls/

# Playwright E2E output
test-results/
playwright-report/

# Subagent-driven-development scratch (briefs, reports, diffs, ledger)
.superpowers/

# Claude Code local settings/worktrees (holds machine-specific paths, LAN IPs,
# and approved-command patterns — never commit to the public repo)
.claude/
